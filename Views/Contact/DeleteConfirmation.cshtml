@model ContactModel
@{
    ViewData["Title"] = "Confirmação para apagar contato";
}

<div class="text-center">
    <h3>Deseja apagar esse usuário (@Model.Name) ?</h3>
    <a class="btn btn-primary  mb-2" role="button" asp-controller="Contact" asp-action="Index">Cancelar exclusão</a>
    <a class="btn btn-danger  mb-2" role="button" asp-controller="Contact" asp-route-id="@Model.Id" asp-action="Delete">Confirmar exclusão</a>
</div>

